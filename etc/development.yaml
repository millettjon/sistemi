# Start a swank server.
swank: true

# Launch the website in a browser.
launch-browser: true

# Paypal sandbox login.
# The password and signature are passed in the environment.
paypal:
  version: "78.0"
  url: "https://api-3t.sandbox.paypal.com/nvp"

# Use the local postgres database.
database-url: postgres://localhost/sistemi
